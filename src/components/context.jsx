import { HashRouter, Routes, Route, Link } from "react-router-dom";
import { createContext } from "react";
const UserContext = createContext(null);


export {HashRouter, Routes, Route, Link, UserContext};